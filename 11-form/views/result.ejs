<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        span {
          color: blue;
          font-weight: 700;
        }
      </style>
</head>
<body>
    <h1><%= title %></h1>
    <div><span><%= userInfo.id %></span>님 환영합니다!</div>
    <div>비밀번호는 <span><%= userInfo.pw %></span>이군요!!</div>

    <!-- 퀴즈. 이메일, 학년 보여주기 -->
    <% if (userInfo.email) { %>
    <div>이메일은 <span><%= userInfo.email %></span></div>
    <% } %>
    <% if (userInfo.grade) { %>
    <div>학년은 <span><%= userInfo.grade %></span> 입니다.</div>
    <% } %>
    <br>
    <br>
    <a href="/">홈으로 이동하기</a>
</body>
</html>